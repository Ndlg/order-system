# Codex Working Notes

- Windows PowerShell has been configured to read and write UTF-8 text in this workspace. When reading or updating Chinese project documents, use UTF-8 directly.
- Do not repeatedly warn the user that terminal Chinese may be garbled. If a command output is garbled, treat it as an environment/configuration issue to fix or work around silently, then continue.
- Preserve all stage taskbooks. Append new stage notes instead of overwriting older reports.
